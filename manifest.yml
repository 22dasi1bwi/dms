applications:
  - name: dms
    instances: 1
    memory: 1024M
    path: backend/target/backend-1.jar
    buildpacks: [https://github.com/cloudfoundry/java-buildpack.git]
    services:
      - dms-db
